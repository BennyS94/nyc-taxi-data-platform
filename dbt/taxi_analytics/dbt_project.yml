name: taxi_analytics
version: 1.0.0
config-version: 2

profile: taxi_analytics

model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  taxi_analytics:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +schema: marts

seeds:
  taxi_analytics:
    +schema: intermediate
